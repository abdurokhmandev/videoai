[build]
builder = "dockerfile"

[deploy]
startCommand = "/bin/sh -c \"uvicorn admin.main:app --host 0.0.0.0 --port $PORT\""
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
